# v1.1 - UI优化 + 手势控制 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现v1.1迭代，包括UI体验优化和手势控制功能

**Architecture:** 分模块实施，先完成UI优化部分，再实现手势控制功能。每个模块独立可测试。

**Tech Stack:** React + TypeScript + Vite + Tailwind CSS + Framer Motion + MediaPipe Hands

---

## 模块一：UI基础优化

### Task 1: 首页文案和布局优化

**Files:**
- Modify: `frontend/src/pages/Home.tsx`

- [ ] **Step 1: 修改文案**

将 `"选择玩法"` 改为 `"选择模式"`：

```typescript
const Home = () => {
  return (
    <div className="min-h-screen">
      <HeroSection />
      <Section title="选择模式" subtitle="多种测算方式，满足你不同的需求">
        <div className="grid grid-cols-1 md:grid-cols-2 gap-8">
          {games.map((game, index) => (
            <GameCard key={index} {...game} />
          ))}
        </div>
      </Section>
    </div>
  )
}
```

- [ ] **Step 2: 修改布局为2x2网格**

修改 grid 类名：
- 移除 `lg:grid-cols-4`
- 改为 `grid grid-cols-1 md:grid-cols-2 gap-8`

- [ ] **Step 3: 验证修改**

确认文件修改后保存。

---

### Task 2: Section组件padding调整

**Files:**
- Modify: `frontend/src/components/Section.tsx`

- [ ] **Step 1: 修改padding**

将 `py-12` 改为 `py-16`：

```typescript
const Section = ({ title, subtitle, children, className }: SectionProps) => {
  return (
    <section className={`py-16 ${className || ''}`}>
      {/* ... 保持其余代码不变 */}
    </section>
  )
}
```

---

### Task 3: Logo组件创建

**Files:**
- Create: `frontend/src/components/Logo.tsx`

- [ ] **Step 1: 创建Logo组件**

```typescript
interface LogoProps {
  size?: 'sm' | 'md' | 'lg'
}

const Logo = ({ size = 'md' }: LogoProps) => {
  const sizeClasses = {
    sm: 'w-8 h-8',
    md: 'w-12 h-12',
    lg: 'w-24 h-24',
  }

  return (
    <div className={`${sizeClasses[size]} relative`}>
      <svg viewBox="0 0 100 100" className="w-full h-full">
        {/* 外圈 - 圆形 */}
        <circle
          cx="50"
          cy="50"
          r="45"
          fill="none"
          stroke="url(#goldGradient)"
          strokeWidth="3"
        />
        {/* 内部"问"字 - 现代简约设计 */}
        <text
          x="50"
          y="58"
          textAnchor="middle"
          fontSize="36"
          fontWeight="500"
          fill="url(#goldGradient)"
          style={{ fontFamily: 'serif' }}
        >
          问
        </text>
        {/* 金色渐变定义 */}
        <defs>
          <linearGradient id="goldGradient" x1="0%" y1="0%" x2="100%" y2="100%">
            <stop offset="0%" stopColor="#f0b429" />
            <stop offset="100%" stopColor="#de911d" />
          </linearGradient>
        </defs>
      </svg>
    </div>
  )
}

export default Logo
```

---

### Task 4: Logo集成到导航栏

**Files:**
- Modify: `frontend/src/components/Layout.tsx`

- [ ] **Step 1: 导入Logo组件**

在文件顶部添加：
```typescript
import Logo from './Logo'
```

- [ ] **Step 2: 修改导航栏，添加Logo**

修改header部分，在"问心"文字前添加Logo：

```typescript
<Link to="/" className="flex items-center gap-2 text-lg font-semibold text-neutral-800 font-serif hover:text-neutral-600">
  <Logo size="sm" />
  问心
</Link>
```

---

### Task 5: Logo集成到首页Hero区域

**Files:**
- Modify: `frontend/src/pages/Home/HeroSection.tsx`

- [ ] **Step 1: 导入Logo组件**

```typescript
import Logo from '../../components/Logo'
```

- [ ] **Step 2: 在标题上方添加大Logo**

```typescript
const HeroSection = () => {
  return (
    <section className="py-20 md:py-32">
      <div className="container-max text-center">
        <div className="flex justify-center mb-6">
          <Logo size="lg" />
        </div>
        <h1 className="text-4xl md:text-6xl font-bold text-neutral-800 font-serif mb-6">
          问心
        </h1>
        {/* ... 其余代码保持不变 */}
      </div>
    </section>
  )
}
```

---

### Task 6: 每日速测卡片配色优化

**Files:**
- Modify: `frontend/src/pages/DailySpeed/MoneyGuaSection.tsx`
- Modify: `frontend/src/pages/DailySpeed/ShengbeiSection.tsx`

- [ ] **Step 1: 修改金钱卦卡片配色**

将 `bg-gradient-to-br from-neutral-100 to-neutral-200` 改为 `bg-gradient-to-br from-yellow-50 to-amber-100`

- [ ] **Step 2: 修改掷圣杯卡片配色**

将 `bg-gradient-to-br from-amber-50 to-orange-100` 改为 `bg-gradient-to-br from-orange-50 to-red-100`

---

### Task 7: 创建SVG图标组件 - 乾隆通宝

**Files:**
- Create: `frontend/src/components/icons/QianlongTongbao.tsx`

- [ ] **Step 1: 创建乾隆通宝SVG组件**

```typescript
interface QianlongTongbaoProps {
  size?: number
  className?: string
}

const QianlongTongbao = ({ size = 64, className = '' }: QianlongTongbaoProps) => {
  return (
    <svg
      viewBox="0 0 100 100"
      width={size}
      height={size}
      className={className}
    >
      <defs>
        <linearGradient id="coinGoldGradient" x1="0%" y1="0%" x2="100%" y2="100%">
          <stop offset="0%" stopColor="#f7c948" />
          <stop offset="50%" stopColor="#f0b429" />
          <stop offset="100%" stopColor="#de911d" />
        </linearGradient>
      </defs>
      
      {/* 外圆 */}
      <circle cx="50" cy="50" r="45" fill="url(#coinGoldGradient)" />
      
      {/* 内方孔 */}
      <rect x="35" y="35" width="30" height="30" fill="#171717" />
      
      {/* 文字 - 乾隆通宝 */}
      <text x="50" y="28" textAnchor="middle" fontSize="14" fill="#8d2b0b" fontWeight="bold">
        乾
      </text>
      <text x="50" y="78" textAnchor="middle" fontSize="14" fill="#8d2b0b" fontWeight="bold">
        隆
      </text>
      <text x="22" y="55" textAnchor="middle" fontSize="14" fill="#8d2b0b" fontWeight="bold">
        通
      </text>
      <text x="78" y="55" textAnchor="middle" fontSize="14" fill="#8d2b0b" fontWeight="bold">
        宝
      </text>
      
      {/* 边缘阴影效果 */}
      <circle cx="50" cy="50" r="45" fill="none" stroke="#cb6e17" strokeWidth="2" />
    </svg>
  )
}

export default QianlongTongbao
```

---

### Task 8: 创建SVG图标组件 - 圣杯

**Files:**
- Create: `frontend/src/components/icons/Shengbei.tsx`

- [ ] **Step 1: 创建圣杯SVG组件**

```typescript
interface ShengbeiProps {
  size?: number
  className?: string
}

const Shengbei = ({ size = 64, className = '' }: ShengbeiProps) => {
  return (
    <svg
      viewBox="0 0 100 100"
      width={size}
      height={size}
      className={className}
    >
      <defs>
        <linearGradient id="cupRedGradient" x1="0%" y1="0%" x2="0%" y2="100%">
          <stop offset="0%" stopColor="#dc2626" />
          <stop offset="100%" stopColor="#7f1d1d" />
        </linearGradient>
      </defs>
      
      {/* 左杯 - 月牙形 */}
      <g transform="translate(20, 25)">
        <path
          d="M 0 25 Q 15 0 30 25 Q 15 30 0 25"
          fill="url(#cupRedGradient)"
          stroke="#450a0a"
          strokeWidth="1.5"
        />
        {/* 木纹线条 */}
        <path d="M 5 15 Q 15 12 25 15" fill="none" stroke="#991b1b" strokeWidth="0.5" />
        <path d="M 8 22 Q 15 20 22 22" fill="none" stroke="#991b1b" strokeWidth="0.5" />
      </g>
      
      {/* 右杯 - 月牙形（镜像） */}
      <g transform="translate(50, 25)">
        <path
          d="M 0 25 Q 15 0 30 25 Q 15 30 0 25"
          fill="url(#cupRedGradient)"
          stroke="#450a0a"
          strokeWidth="1.5"
          transform="scale(-1, 1) translate(-30, 0)"
        />
        {/* 木纹线条 */}
        <path d="M 5 15 Q 15 12 25 15" fill="none" stroke="#991b1b" strokeWidth="0.5" transform="scale(-1, 1) translate(-30, 0)" />
        <path d="M 8 22 Q 15 20 22 22" fill="none" stroke="#991b1b" strokeWidth="0.5" transform="scale(-1, 1) translate(-30, 0)" />
      </g>
    </svg>
  )
}

export default Shengbei
```

---

### Task 9: 替换卡片中的emoji图标为SVG图标

**Files:**
- Modify: `frontend/src/pages/DailySpeed/MoneyGuaSection.tsx`
- Modify: `frontend/src/pages/DailySpeed/ShengbeiSection.tsx`

- [ ] **Step 1: 修改MoneyGuaSection**

导入图标：
```typescript
import QianlongTongbao from '../../components/icons/QianlongTongbao'
```

替换 `<div className="text-6xl mb-4">🪙</div>` 为：
```typescript
<div className="mb-4">
  <QianlongTongbao size={72} />
</div>
```

- [ ] **Step 2: 修改ShengbeiSection**

导入图标：
```typescript
import Shengbei from '../../components/icons/Shengbei'
```

替换 `<div className="text-6xl mb-4">🏺</div>` 为：
```typescript
<div className="mb-4">
  <Shengbei size={72} />
</div>
```

---

## 模块二：冷却机制

### Task 10: 创建冷却提示组件

**Files:**
- Create: `frontend/src/components/CoolDownTooltip.tsx`

- [ ] **Step 1: 创建气泡提示组件**

```typescript
import { useEffect, useState } from 'react'
import { motion, AnimatePresence } from 'framer-motion'

interface CoolDownTooltipProps {
  isVisible: boolean
  message: string
  onClose: () => void
}

const CoolDownTooltip = ({ isVisible, message, onClose }: CoolDownTooltipProps) => {
  useEffect(() => {
    if (isVisible) {
      const timer = setTimeout(() => {
        onClose()
      }, 3000)
      return () => clearTimeout(timer)
    }
  }, [isVisible, onClose])

  return (
    <AnimatePresence>
      {isVisible && (
        <motion.div
          initial={{ opacity: 0, y: 10 }}
          animate={{ opacity: 1, y: 0 }}
          exit={{ opacity: 0, y: 10 }}
          className="absolute -top-16 left-1/2 -translate-x-1/2 z-10"
        >
          <div className="bg-neutral-800 text-white px-4 py-3 rounded-lg shadow-lg text-center max-w-xs">
            <p className="text-sm">{message}</p>
          </div>
          {/* 小箭头 */}
          <div className="absolute -bottom-2 left-1/2 -translate-x-1/2 border-8 border-transparent border-t-neutral-800" />
        </motion.div>
      )}
    </AnimatePresence>
  )
}

export default CoolDownTooltip
```

---

### Task 11: 集成冷却逻辑到DailySpeed页面

**Files:**
- Modify: `frontend/src/pages/DailySpeed.tsx`

- [ ] **Step 1: 添加冷却状态**

```typescript
import { useState, useCallback } from 'react'
import CoolDownTooltip from '../components/CoolDownTooltip'
```

- [ ] **Step 2: 添加冷却状态变量**

```typescript
const DailySpeed = () => {
  const [moneyGuaOpen, setMoneyGuaOpen] = useState(false)
  const [shengbeiOpen, setShengbeiOpen] = useState(false)
  const [lastDivinationTime, setLastDivinationTime] = useState<number | null>(null)
  const [coolDownTooltip, setCoolDownTooltip] = useState<{ visible: boolean; message: string }>({
    visible: false,
    message: '',
  })
```

- [ ] **Step 3: 添加冷却检查函数**

```typescript
  const checkCoolDown = useCallback((): boolean => {
    if (lastDivinationTime === null) {
      return true // 可以进行
    }
    const elapsed = Date.now() - lastDivinationTime
    const remaining = Math.ceil((30000 - elapsed) / 1000)
    if (remaining > 0) {
      setCoolDownTooltip({
        visible: true,
        message: `一卦一问方显诚，心定再卜，请静心等候 ${remaining}s`,
      })
      return false // 还在冷却中
    }
    return true // 可以进行
  }, [lastDivinationTime])
```

- [ ] **Step 4: 修改打开弹窗的函数**

```typescript
  const handleMoneyGuaStart = useCallback(() => {
    if (checkCoolDown()) {
      setMoneyGuaOpen(true)
    }
  }, [checkCoolDown])

  const handleShengbeiStart = useCallback(() => {
    if (checkCoolDown()) {
      setShengbeiOpen(true)
    }
  }, [checkCoolDown])
```

- [ ] **Step 5: 添加测算完成的回调**

```typescript
  const handleDivinationComplete = useCallback(() => {
    setLastDivinationTime(Date.now())
  }, [])
```

- [ ] **Step 6: 修改JSX，传递回调并添加提示组件**

```typescript
return (
  <Layout showBackButton>
    <div className="container-max py-12">
      <Section title="每日速测" subtitle="一分钟速测问吉凶">
        <div className="flex flex-col md:flex-row gap-6 min-h-[400px]">
          <div className="flex-1 relative">
            <MoneyGuaSection onStart={handleMoneyGuaStart} />
            <CoolDownTooltip
              isVisible={coolDownTooltip.visible}
              message={coolDownTooltip.message}
              onClose={() => setCoolDownTooltip({ ...coolDownTooltip, visible: false })}
            />
          </div>
          <div className="flex-1 relative">
            <ShengbeiSection onStart={handleShengbeiStart} />
          </div>
        </div>
      </Section>
    </div>
    <MoneyGuaModal
      isOpen={moneyGuaOpen}
      onClose={() => setMoneyGuaOpen(false)}
      onComplete={handleDivinationComplete}
    />
    <ShengbeiModal
      isOpen={shengbeiOpen}
      onClose={() => setShengbeiOpen(false)}
      onComplete={handleDivinationComplete}
    />
  </Layout>
)
```

---

### Task 12: 修改Modal组件支持完成回调

**Files:**
- Modify: `frontend/src/pages/DailySpeed/MoneyGuaModal.tsx`
- Modify: `frontend/src/pages/DailySpeed/ShengbeiModal.tsx`

- [ ] **Step 1: 修改MoneyGuaModal props**

```typescript
interface MoneyGuaModalProps {
  isOpen: boolean
  onClose: () => void
  onComplete?: () => void
}
```

- [ ] **Step 2: 在显示结果时调用onComplete**

在 `step === 'result'` 的分支中，添加 `useEffect` 或在进入result时调用：

```typescript
// 在step变为'result'时调用onComplete
useEffect(() => {
  if (step === 'result' && onComplete) {
    onComplete()
  }
}, [step, onComplete])
```

- [ ] **Step 3: 对ShengbeiModal做同样修改**

重复Step 1-2，修改ShengbeiModal组件。

---

## 模块三：手势控制（核心功能）

### Task 13: 安装MediaPipe依赖

**Files:**
- Modify: `frontend/package.json`

- [ ] **Step 1: 安装MediaPipe相关包**

```bash
cd frontend
npm install @mediapipe/hands
```

---

### Task 14: 创建手势识别Hook

**Files:**
- Create: `frontend/src/hooks/useGestureControl.ts`

- [ ] **Step 1: 创建基础Hook结构**

```typescript
import { useEffect, useRef, useState, useCallback } from 'react'
import { Hands } from '@mediapipe/hands'

export type GestureType = 'shake' | 'throw_up' | null

interface UseGestureControlOptions {
  enabled: boolean
  onGestureDetected: (gesture: GestureType) => void
  targetGesture?: 'shake' | 'throw_up'
}

export const useGestureControl = ({
  enabled,
  onGestureDetected,
  targetGesture,
}: UseGestureControlOptions) => {
  const videoRef = useRef<HTMLVideoElement>(null)
  const canvasRef = useRef<HTMLCanvasElement>(null)
  const handsRef = useRef<Hands | null>(null)
  const [cameraReady, setCameraReady] = useState(false)
  const [isDetecting, setIsDetecting] = useState(false)
  
  // 用于手势检测的状态
  const lastLandmarksRef = useRef<any[]>([])
  const shakeDetectRef = useRef({
    lastY: 0,
    shakeCount: 0,
    lastShakeTime: 0,
  })
  const throwUpDetectRef = useRef({
    isRising: false,
    startY: 0,
    startTime: 0,
  })

  // 初始化MediaPipe Hands
  useEffect(() => {
    if (!enabled) return

    const hands = new Hands({
      locateFile: (file) => {
        return `https://cdn.jsdelivr.net/npm/@mediapipe/hands/${file}`
      },
    })

    hands.setOptions({
      maxNumHands: 1,
      modelComplexity: 1,
      minDetectionConfidence: 0.7,
      minTrackingConfidence: 0.5,
    })

    handsRef.current = hands

    return () => {
      hands.close()
    }
  }, [enabled])

  // 启动摄像头
  const startCamera = useCallback(async () => {
    try {
      const stream = await navigator.mediaDevices.getUserMedia({
        video: { facingMode: 'user' },
      })
      
      if (videoRef.current) {
        videoRef.current.srcObject = stream
        videoRef.current.play()
        setCameraReady(true)
      }
    } catch (error) {
      console.error('Camera error:', error)
      throw error
    }
  }, [])

  // 停止摄像头
  const stopCamera = useCallback(() => {
    if (videoRef.current?.srcObject) {
      const stream = videoRef.current.srcObject as MediaStream
      stream.getTracks().forEach(track => track.stop())
    }
    setCameraReady(false)
  }, [])

  // 检测摇铜钱手势
  const detectShakeGesture = useCallback((landmarks: any[]) => {
    const wrist = landmarks[0]
    const now = Date.now()
    const state = shakeDetectRef.current
    
    // 检测手腕的上下/左右移动
    if (state.lastY !== 0) {
      const deltaY = Math.abs(wrist.y - state.lastY)
      
      if (deltaY > 0.08 && now - state.lastShakeTime > 200) {
        state.shakeCount++
        state.lastShakeTime = now
        
        // 连续摇晃3次以上触发
        if (state.shakeCount >= 3) {
          shakeDetectRef.current = {
            lastY: 0,
            shakeCount: 0,
            lastShakeTime: 0,
          }
          return true
        }
      }
    }
    
    state.lastY = wrist.y
    
    // 重置计数（如果超过1秒没动作）
    if (now - state.lastShakeTime > 1000) {
      state.shakeCount = 0
    }
    
    return false
  }, [])

  // 检测抛圣杯手势
  const detectThrowUpGesture = useCallback((landmarks: any[]) => {
    const wrist = landmarks[0]
    const now = Date.now()
    const state = throwUpDetectRef.current
    
    if (!state.isRising) {
      // 等待手腕向下到低位
      if (wrist.y > 0.7) {
        state.isRising = true
        state.startY = wrist.y
        state.startTime = now
      }
    } else {
      // 检测快速向上移动
      const deltaY = state.startY - wrist.y
      const deltaTime = now - state.startTime
      
      if (deltaY > 0.2 && deltaTime < 500) {
        // 成功检测到上抛手势
        throwUpDetectRef.current = {
          isRising: false,
          startY: 0,
          startTime: 0,
        }
        return true
      }
      
      // 超时重置
      if (deltaTime > 1000) {
        state.isRising = false
      }
    }
    
    return false
  }, [])

  // 处理检测结果
  const onResults = useCallback((results: any) => {
    if (!results.multiHandLandmarks || results.multiHandLandmarks.length === 0) {
      return
    }

    const landmarks = results.multiHandLandmarks[0]
    
    if (targetGesture === 'shake') {
      if (detectShakeGesture(landmarks)) {
        onGestureDetected('shake')
      }
    } else if (targetGesture === 'throw_up') {
      if (detectThrowUpGesture(landmarks)) {
        onGestureDetected('throw_up')
      }
    }
  }, [targetGesture, detectShakeGesture, detectThrowUpGesture, onGestureDetected])

  // 开始手势检测
  const startDetection = useCallback(async () => {
    if (!handsRef.current || !videoRef.current) return
    
    setIsDetecting(true)
    
    handsRef.current.onResults(onResults)
    
    // 简单的帧循环
    const renderFrame = async () => {
      if (!videoRef.current || !handsRef.current || !isDetecting) return
      
      await handsRef.current.send({ image: videoRef.current })
      requestAnimationFrame(renderFrame)
    }
    
    renderFrame()
  }, [onResults, isDetecting])

  // 停止手势检测
  const stopDetection = useCallback(() => {
    setIsDetecting(false)
  }, [])

  return {
    videoRef,
    canvasRef,
    cameraReady,
    isDetecting,
    startCamera,
    stopCamera,
    startDetection,
    stopDetection,
  }
}
```

---

### Task 15: 创建手势摄像头组件

**Files:**
- Create: `frontend/src/components/GestureCamera.tsx`

- [ ] **Step 1: 创建摄像头组件**

```typescript
import { useEffect, useRef } from 'react'
import { motion } from 'framer-motion'
import QianlongTongbao from './icons/QianlongTongbao'
import Shengbei from './icons/Shengbei'

interface GestureCameraProps {
  videoRef: React.RefObject<HTMLVideoElement>
  type: 'money_gua' | 'shengbei'
  status: 'waiting' | 'detecting' | 'success'
  gestureInstruction: string
  onAnimationComplete?: () => void
}

const GestureCamera = ({
  videoRef,
  type,
  status,
  gestureInstruction,
  onAnimationComplete,
}: GestureCameraProps) => {
  const containerRef = useRef<HTMLDivElement>(null)

  // 道具动画
  useEffect(() => {
    if (status === 'success' && onAnimationComplete) {
      const timer = setTimeout(() => {
        onAnimationComplete()
      }, 1500)
      return () => clearTimeout(timer)
    }
  }, [status, onAnimationComplete])

  return (
    <div ref={containerRef} className="relative w-full aspect-video bg-neutral-900 rounded-xl overflow-hidden">
      {/* 摄像头画面 */}
      <video
        ref={videoRef}
        className="w-full h-full object-cover"
        playsInline
        muted
      />
      
      {/* 叠加层 */}
      <div className="absolute inset-0 bg-gradient-to-t from-neutral-900/60 via-transparent to-neutral-900/30">
        {/* 道具图案 */}
        <div className="absolute top-4 right-4">
          {type === 'money_gua' ? (
            <div className="flex gap-1">
              <motion.div
                animate={status === 'success' ? {
                  y: [0, -20, 0, -15, 0],
                  rotate: [0, 180, 360, 540, 720],
                } : {}}
                transition={{ duration: 1.5 }}
              >
                <QianlongTongbao size={40} />
              </motion.div>
              <motion.div
                animate={status === 'success' ? {
                  y: [0, -20, 0, -15, 0],
                  rotate: [0, 180, 360, 540, 720],
                } : {}}
                transition={{ duration: 1.5, delay: 0.1 }}
              >
                <QianlongTongbao size={40} />
              </motion.div>
              <motion.div
                animate={status === 'success' ? {
                  y: [0, -20, 0, -15, 0],
                  rotate: [0, 180, 360, 540, 720],
                } : {}}
                transition={{ duration: 1.5, delay: 0.2 }}
              >
                <QianlongTongbao size={40} />
              </motion.div>
            </div>
          ) : (
            <motion.div
              animate={status === 'success' ? {
                y: [0, -60, 0],
              } : {}}
              transition={{ duration: 1 }}
            >
              <Shengbei size={60} />
            </motion.div>
          )}
        </div>
        
        {/* 简笔画手势示意图 */}
        <div className="absolute bottom-4 left-4 bg-white/90 rounded-lg p-3">
          <div className="text-xs text-neutral-600 mb-1">手势示意：</div>
          <div className="text-2xl">
            {type === 'money_gua' ? '✊ ↕️' : '🖐️⬆️'}
          </div>
        </div>
      </div>
      
      {/* 状态提示 */}
      <div className="absolute bottom-0 left-0 right-0 p-4">
        <div className="bg-white/95 rounded-lg p-4 text-center">
          {status === 'waiting' && (
            <p className="text-neutral-800 whitespace-pre-line">{gestureInstruction}</p>
          )}
          {status === 'detecting' && (
            <p className="text-neutral-800">识别中...</p>
          )}
          {status === 'success' && (
            <p className="text-green-700 font-semibold">识别成功！</p>
          )}
        </div>
      </div>
    </div>
  )
}

export default GestureCamera
```

---

### Task 16: 重写MoneyGuaModal集成手势控制

**Files:**
- Modify: `frontend/src/pages/DailySpeed/MoneyGuaModal.tsx`

这个改动比较大，建议重新创建组件，集成手势模式和点击模式两种方式。

- [ ] **Step 1: 更新组件完整代码**

```typescript
import { useState, useEffect } from 'react'
import { motion, AnimatePresence } from 'framer-motion'
import Modal from '../../components/Modal'
import Button from '../../components/Button'
import Input from '../../components/Input'
import GestureCamera from '../../components/GestureCamera'
import { useMoneyGua } from '../../hooks/useMoneyGua'
import { useGestureControl } from '../../hooks/useGestureControl'
import type { MoneyGuaResult } from '../../utils/moneyGua'

interface MoneyGuaModalProps {
  isOpen: boolean
  onClose: () => void
  onComplete?: () => void
}

type ModalMode = 'input' | 'permission' | 'gesture_waiting' | 'gesture_detecting' | 'animating' | 'result' | 'click_tossing'

const CoinAnimation = () => {
  return (
    <div className="flex gap-4 justify-center py-8">
      {[0, 1, 2].map((i) => (
        <motion.div
          key={i}
          animate={{
            y: [0, -30, 0, -20, 0],
            rotate: [0, 180, 360, 540, 720],
          }}
          transition={{
            duration: 1.5,
            repeat: Infinity,
            delay: i * 0.1,
          }}
          className="w-16 h-16 rounded-full bg-gradient-to-br from-yellow-400 to-yellow-600 flex items-center justify-center text-yellow-900 font-bold shadow-lg"
        >
          乾隆
        </motion.div>
      ))}
    </div>
  )
}

const ResultDisplay = ({ result }: { result: MoneyGuaResult }) => {
  return (
    <motion.div
      initial={{ opacity: 0, y: 20 }}
      animate={{ opacity: 1, y: 0 }}
      className="text-center"
    >
      <div className="flex gap-4 justify-center mb-6">
        {result.coins.map((coin, index) => (
          <div
            key={index}
            className={`w-16 h-16 rounded-full flex items-center justify-center font-bold shadow-lg ${
              coin.side === 'yang'
                ? 'bg-gradient-to-br from-yellow-400 to-yellow-600 text-yellow-900'
                : 'bg-gradient-to-br from-neutral-600 to-neutral-800 text-white'
            }`}
          >
            {coin.side === 'yang' ? '乾隆' : '通宝'}
          </div>
        ))}
      </div>
      <div className="mb-4">
        <span className={`inline-block px-6 py-2 rounded-full text-lg font-semibold ${
          result.verdict.includes('大')
            ? result.verdict.includes('吉')
              ? 'bg-green-100 text-green-800'
              : 'bg-red-100 text-red-800'
            : result.verdict === '吉'
            ? 'bg-green-50 text-green-700'
            : 'bg-neutral-100 text-neutral-700'
        }`}>
          {result.verdict}
        </span>
      </div>
      <p className="text-neutral-600 leading-relaxed">
        {result.interpretation}
      </p>
    </motion.div>
  )
}

const MoneyGuaModal = ({ isOpen, onClose, onComplete }: MoneyGuaModalProps) => {
  const [question, setQuestion] = useState('')
  const [mode, setMode] = useState<ModalMode>('input')
  const [cameraError, setCameraError] = useState('')
  const { result, toss, reset } = useMoneyGua()
  
  const {
    videoRef,
    cameraReady,
    isDetecting,
    startCamera,
    stopCamera,
    startDetection,
    stopDetection,
  } = useGestureControl({
    enabled: isOpen,
    targetGesture: 'shake',
    onGestureDetected: async (gesture) => {
      if (gesture === 'shake' && mode === 'gesture_waiting') {
        setMode('gesture_detecting')
        await toss()
        setMode('animating')
      }
    },
  })

  // 调用onComplete
  useEffect(() => {
    if ((mode === 'result' || mode === 'animating') && onComplete) {
      onComplete()
    }
  }, [mode, onComplete])

  // 动画完成后显示结果
  const handleAnimationComplete = () => {
    if (result) {
      setMode('result')
    }
  }

  // 启动摄像头
  const handleStartCamera = async () => {
    try {
      setCameraError('')
      await startCamera()
      setMode('gesture_waiting')
    } catch (err) {
      setCameraError('无法访问摄像头，请检查权限设置')
    }
  }

  // 手势模式的测算
  const handleGestureToss = async () => {
    setMode('gesture_detecting')
    await toss()
    setMode('animating')
  }

  // 点击模式的测算
  const handleClickStart = async () => {
    setMode('click_tossing')
    await toss()
    setMode('result')
  }

  const handleReset = () => {
    reset()
    setQuestion('')
    setMode('input')
    setCameraError('')
    stopCamera()
    stopDetection()
  }

  const handleClose = () => {
    handleReset()
    onClose()
  }

  const gestureInstruction = `请做「摇铜钱」手势：

手握拳，在胸前上下或左右摇晃
（就像手里握着铜钱在摇一样）`

  return (
    <Modal isOpen={isOpen} onClose={handleClose} title="金钱卦" hideCloseButton={mode === 'gesture_detecting' || mode === 'animating'}>
      <AnimatePresence mode="wait">
        {mode === 'input' && (
          <motion.div
            key="input"
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            exit={{ opacity: 0 }}
          >
            <p className="text-neutral-600 mb-6">
              请默念心中所想之事，然后选择测算方式。
            </p>
            <Input
              label="所求之事（可选）"
              placeholder="请输入你想测算的问题..."
              value={question}
              onChange={(e) => setQuestion(e.target.value)}
              className="mb-6"
            />
            {question && (
              <p className="text-sm text-neutral-500 mb-6">
                输入问题后，本次测算将被记录，方便后续查看。
              </p>
            )}
            <div className="grid grid-cols-2 gap-4 mb-6">
              <button
                onClick={handleStartCamera}
                className="p-6 rounded-xl border-2 border-neutral-200 hover:border-neutral-800 transition-colors text-center"
              >
                <div className="text-3xl mb-2">📷</div>
                <div className="font-semibold text-neutral-800">手势测算</div>
                <div className="text-sm text-neutral-500">摇铜钱触发</div>
              </button>
              <button
                onClick={handleClickStart}
                className="p-6 rounded-xl border-2 border-neutral-200 hover:border-neutral-800 transition-colors text-center"
              >
                <div className="text-3xl mb-2">👆</div>
                <div className="font-semibold text-neutral-800">点击测算</div>
                <div className="text-sm text-neutral-500">直接开始</div>
              </button>
            </div>
            {cameraError && (
              <p className="text-red-600 text-sm mb-4">{cameraError}</p>
            )}
            <div className="flex gap-3 justify-end">
              <Button variant="secondary" onClick={handleClose}>
                取消
              </Button>
            </div>
          </motion.div>
        )}

        {(mode === 'gesture_waiting' || mode === 'gesture_detecting' || mode === 'animating') && (
          <motion.div
            key="gesture"
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            exit={{ opacity: 0 }}
          >
            <GestureCamera
              videoRef={videoRef}
              type="money_gua"
              status={
                mode === 'gesture_waiting' ? 'waiting' :
                mode === 'gesture_detecting' ? 'detecting' : 'success'
              }
              gestureInstruction={gestureInstruction}
              onAnimationComplete={handleAnimationComplete}
            />
            <div className="mt-4 flex justify-center">
              <Button variant="secondary" onClick={handleReset}>
                换一种方式
              </Button>
            </div>
            {mode === 'gesture_waiting' && (
              <div className="mt-4 flex justify-center">
                <Button onClick={handleGestureToss}>
                  直接开始（跳过手势）
                </Button>
              </div>
            )}
          </motion.div>
        )}

        {mode === 'click_tossing' && (
          <motion.div
            key="tossing"
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            exit={{ opacity: 0 }}
            className="text-center py-4"
          >
            <CoinAnimation />
            <p className="text-neutral-600">正在掷铜钱...</p>
          </motion.div>
        )}

        {mode === 'result' && result && (
          <motion.div
            key="result"
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            exit={{ opacity: 0 }}
          >
            <ResultDisplay result={result} />
            <div className="flex gap-3 justify-center mt-8">
              <Button variant="secondary" onClick={handleReset}>
                再测一次
              </Button>
              <Button onClick={handleClose}>
                完成
              </Button>
            </div>
          </motion.div>
        )}
      </AnimatePresence>
    </Modal>
  )
}

export default MoneyGuaModal
```

---

### Task 17: 重写ShengbeiModal集成手势控制

**Files:**
- Modify: `frontend/src/pages/DailySpeed/ShengbeiModal.tsx`

这个和MoneyGuaModal类似，使用throw_up手势。

- [ ] **Step 1: 按照MoneyGuaModal的模式重写ShengbeiModal**

主要区别：
- targetGesture改为'throw_up'
- 道具用Shengbei图标
- 手势提示文字改为抛圣杯的说明

---

## 模块四：测试与验证

### Task 18: 运行构建验证

**Files:** （无新文件）

- [ ] **Step 1: 安装依赖（如果还没安装MediaPipe）**

```bash
cd frontend
npm install
```

- [ ] **Step 2: 运行TypeScript检查**

```bash
cd frontend
npx tsc --noEmit
```

预期：无TypeScript错误

- [ ] **Step 3: 运行构建**

```bash
cd frontend
npm run build
```

预期：构建成功，无错误

---

### Task 19: 功能测试清单

**Files:** （无新文件）

- [ ] **Step 1: 手动测试UI优化部分**
  - [ ] 首页文案显示"选择模式"
  - [ ] 首页卡片为2x2布局
  - [ ] Logo在导航栏和首页Hero区域显示
  - [ ] 每日速测卡片配色为暖色调
  - [ ] SVG图标正确显示

- [ ] **Step 2: 手动测试冷却机制**
  - [ ] 测算后30秒内点击有提示
  - [ ] 30秒后可以正常使用
  - [ ] 提示3秒后自动消失

- [ ] **Step 3: 手动测试手势控制（降级模式）**
  - [ ] 可以选择点击模式正常使用
  - [ ] 摄像头权限被拒绝时有降级方案

---

## 验收标准

所有任务完成后，验证：
- ✅ 所有UI优化需求已实现
- ✅ 冷却机制正常工作
- ✅ 手势控制功能可用，并有完善的降级方案
- ✅ 代码无TypeScript错误
- ✅ 构建成功
- ✅ 所有验收清单项目已勾选

---

## 后续优化（v1.2）

- 手机摄像头联动
- 更多手势识别优化
- 用户体验微调
