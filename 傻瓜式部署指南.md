# 🚀 傻瓜式部署指南 - 复制粘贴就能用！

这个指南会让你在 15 分钟内把网站部署好，发给朋友玩！

---

## 📦 第一步：把代码推送到 GitHub

### 1.1 如果还没初始化 Git

```bash
git init
git add .
git commit -m "初始化项目"
```

### 1.2 在 GitHub 创建新仓库

1. 访问 https://github.com/new
2. 仓库名填：`wenxin`（或你喜欢的名字）
3. 选择 **Public** 或 **Private** 都可以
4. **不要**勾选 "Add a README"、"Add .gitignore"
5. 点击 "Create repository"

### 1.3 推送代码

按照 GitHub 页面显示的命令操作，类似：

```bash
git remote add origin https://github.com/你的用户名/wenxin.git
git branch -M main
git push -u origin main
```

✅ 代码已上传到 GitHub！

---

## 🎨 第二步：部署前端到 Vercel

### 2.1 打开 Vercel

1. 访问 https://vercel.com
2. 点击 "Sign Up"，用 **GitHub 账号**登录
3. 授权 Vercel 访问你的 GitHub 仓库

### 2.2 导入项目

1. 点击 "New Project"
2. 找到你刚推的 `wenxin` 仓库，点击 "Import"

### 2.3 配置项目

在 "Configure Project" 页面：

| 配置项 | 填什么 |
|--------|--------|
| **Project Name** | wenxin（或你喜欢的） |
| **Root Directory** | 点击 "Edit" → 选 `frontend` |
| **Framework Preset** | 会自动识别为 Vite，不用改 |
| **Build Command** | 不用改 |
| **Output Directory** | 不用改 |

### 2.4 添加环境变量（重要！）

在 "Environment Variables" 区域，点击 "Add"：

1. **Name**: `VITE_SUPABASE_URL`
   **Value**: 复制你本地 `.env.local` 里的这个值

2. **Name**: `VITE_SUPABASE_ANON_KEY`
   **Value**: 复制你本地 `.env.local` 里的这个值

3. **Name**: `VITE_API_BASE_URL`
   **Value**: 先填 `http://localhost:8000`（后面部署后端后回来改）

### 2.5 部署！

点击 "Deploy" 按钮

⏰ 等待 1-2 分钟...

### 2.6 完成！

看到 "Congratulations!" 就成功了！

🎯 复制你的前端地址，类似：`https://wenxin-abc123.vercel.app`

保存好这个地址，后面要用！

---

## 🔧 第三步：部署后端到 Render

### 3.1 打开 Render

1. 访问 https://render.com
2. 点击 "Get Started"，用 **GitHub 账号**登录
3. 授权 Render 访问你的 GitHub 仓库

### 3.2 创建 Web Service

1. 点击右上角的 "New +"
2. 选择 "Web Service"

### 3.3 连接仓库

1. 找到你的 `wenxin` 仓库，点击 "Connect"

### 3.4 配置后端

| 配置项 | 填什么 |
|--------|--------|
| **Name** | wenxin-backend |
| **Region** | Singapore（选离中国近的） |
| **Root Directory** | 点击 "Edit" → 选 `backend` |
| **Runtime** | Python 3 |
| **Build Command** | `pip install -r requirements.txt` |
| **Start Command** | `uvicorn main:app --host 0.0.0.0 --port 10000` |
| **Instance Type** | Free |

### 3.5 添加环境变量（最重要！）

在 "Environment Variables" 区域，点击 "Add Environment Variable"，把你本地 `backend/.env` 里的所有变量都加进去：

```env
# 需要添加的变量：
SUPABASE_URL=...
SUPABASE_SERVICE_KEY=...
KIMI_API_KEY=...
# ... 等等，把你 .env 里的都复制过来

# 额外加这两个：
FRONTEND_URL=https://wenxin-abc123.vercel.app  ← 换成你第二步得到的前端地址
PYTHON_VERSION=3.11.0
```

### 3.6 部署！

点击 "Create Web Service"

⏰ 等待 3-5 分钟...（第一次比较慢）

### 3.7 完成！

看到 "Live" 就成功了！

🎯 复制你的后端地址，类似：`https://wenxin-backend-abc123.onrender.com`

保存好这个地址！

---

## 🔗 第四步：前后端连接

### 4.1 更新前端环境变量

回到 Vercel：

1. 进入你的项目 → Settings → Environment Variables
2. 找到 `VITE_API_BASE_URL`，点击 "Edit"
3. 把值改成你第三步得到的后端地址，例如：
   ```
   https://wenxin-backend-abc123.onrender.com
   ```
4. 点击 "Save"

### 4.2 重新部署前端

1. 回到 Vercel 项目首页
2. 点击 "Deployments" 标签
3. 找到最新的部署，点击右边的 ... → "Redeploy"
4. 确认点击 "Redeploy"

⏰ 等待 1 分钟...

---

## 🎉 第五步：测试一下！

1. 访问你的前端地址：`https://wenxin-abc123.vercel.app`
2. 试试抽签功能
3. 试试解读功能
4. 如果都正常，恭喜你！🥳

---

## 📤 分享给朋友！

把你的前端地址发给朋友：

```
🔗 来玩我的运势测算网站：
https://wenxin-abc123.vercel.app
```

---

## ⚠️ 注意事项

### Render 免费版的小问题

- **15分钟休眠**：如果没人访问，后端会休眠。第一次访问可能需要 5-10 秒启动。
- **每月750小时限制**：够个人使用。

### 解决休眠问题（可选）

可以用 [UptimeRobot](https://uptimerobot.com) 每 5 分钟访问一次你的后端，保持它醒着。

---

## 🔍 出问题了？

| 问题 | 检查什么 |
|------|---------|
| 前端打不开 | 检查 Vercel 部署日志 |
| 抽签没反应 | 检查前端控制台的网络请求 |
| 解读失败 | 检查 Render 的日志，看环境变量是否正确 |
| CORS 错误 | 检查后端的 `FRONTEND_URL` 环境变量 |

---

## 📞 需要帮助？

- Vercel 文档：https://vercel.com/docs
- Render 文档：https://render.com/docs
- Supabase 文档：https://supabase.com/docs

---

**祝你部署顺利！🎊**
